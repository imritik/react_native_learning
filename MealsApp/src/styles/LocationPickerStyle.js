import {StyleSheet} from 'react-native';
export default StyleSheet.create({
  actions: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    width: '100%',
    marginBottom: 10,
  },
});
